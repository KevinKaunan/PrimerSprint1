<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.primersprint">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity android:name=".ui.GridAlbumActivity"></activity>
        <activity android:name=".AlbumesActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".ui.EditarPerfilActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".InfoActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".RegistrarActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".CamaraActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GridItemActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity
            android:windowSoftInputMode="adjustPan"
            android:name=".PerfilActivity"
            android:label="@string/title_activity__perfil"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:windowSoftInputMode="adjustPan"
            android:name=".MainActivity"
            android:label="@string/app_name" />
    </application>

</manifest>